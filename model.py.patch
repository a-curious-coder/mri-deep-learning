# Patch generated by Pyment v0.3.3

--- a/model.py
+++ b/model.py
@@ -1,13 +1,14 @@
 # Class Encapsulating classifier functions
 class Model:
     """
-    Args:
-        x_train: training features
-        x_test: testing features
-        y_train: training targets
-        y_test:  testing targets
-        model: classifier to be trained
-        name: string representing model name
+
+    :param x_train: training features
+    :param x_test: testing features
+    :param y_train: training targets
+    :param y_test: testing targets
+    :param model: classifier to be trained
+    :param name: string representing model name
+
     """
 
     def __init__(self, x_train, x_test, y_train, y_test, model, name):
@@ -23,10 +24,9 @@
     def train_and_fit(self, grid=None):
         """
 
-        Parameters
-        ----------
-        grid : _type_, optional
-            _description_, by default None
+        :param grid:  (Default value = None)
+
+        
         """
 
         # Only apply RandomSearch if pass as relevant argument
@@ -50,6 +50,7 @@
     '''
 
     def print_metrics(self):
+        """ """
         print(self.name)
 
         print("METRICS")
@@ -65,6 +66,11 @@
     '''
 
     def plot_precision_recall(self, f_name=None):
+        """
+
+        :param f_name:  (Default value = None)
+
+        """
 
         PrecisionRecallDisplay.from_predictions(self.y_test, self.predictions)
 
@@ -80,6 +86,11 @@
     '''
 
     def plot_confusion_matrix(self, f_name=None):
+        """
+
+        :param f_name:  (Default value = None)
+
+        """
 
         cm = confusion_matrix(self.y_test, self.predictions,
                               labels=self.model.classes_)
